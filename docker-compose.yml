services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      NITRO_HOST: 0.0.0.0
      NITRO_PORT: 3000
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    networks:
      - polaris

networks:
  polaris:
    name: polaris
